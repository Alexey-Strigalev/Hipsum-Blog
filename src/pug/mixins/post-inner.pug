mixin post-inner(title, info, img, link="./blog-page.html")
    article.post-inner
        h2.post__title #{title}
        .post__info #{info}
        .post__img
            img(src="./img/posts-preview/" + img, alt= title )
        .post__content
            block